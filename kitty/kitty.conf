# font_family Iosevka Custom
font_family Berkeley Mono
bold_font        auto

italic_font      auto
bold_italic_font auto

font_size 10.0

disable_ligatures never
cursor_trail 1

# Window configuration
hide_window_decorations yes
initial_window_width 90c
initial_window_height 25c
remember_window_size yes
# Allow kitty to save the window size
# Visual enhancements
window_padding_width 10
window_margin_width 2
single_window_margin_width 0
draw_minimal_borders yes
# active_border_color #ff69b4
# inactive_border_color #444444
window_border_width 1pt

# Cursor customization
cursor_shape block
cursor_blink_interval 0

# Mouse and selection
copy_on_select yes

# Scrolling
scrollback_lines 10000

# Keyboard mappings
map ctrl+q no_op
map ctrl+shift+c copy_to_clipboard
map ctrl+shift+v paste_from_clipboard
map ctrl+shift+enter launch --cwd current

# Font size zoom
map ctrl+plus       change_font_size all +2.0
map ctrl+equal      change_font_size all +2.0
map ctrl+minus      change_font_size all -2.0
map ctrl+0          change_font_size all 0

# Performance
repaint_delay 10
input_delay 3
sync_to_monitor no
url_style curly

# Bell
enable_audio_bell no

# Tab management
tab_bar_style powerline
tab_powerline_style slanted
tab_bar_min_tabs 2
tab_bar_edge top

# Claude Code status pills. The hook ~/.claude/hooks/kitty-status.sh sets one of
# four user_vars (cc_status_running/waiting/done/ready) to a padded label string
# (" RUNNING " etc.) and clears the others. Each pill renders only when its
# user_var is non-empty, so only the active state is visible.
tab_title_template "{index}: {title}{fmt.bg.green}{fmt.fg.black}{tab.active_oldest_window.user_vars.cc_status_running}{fmt.bg.default}{fmt.fg.default}{fmt.bg.yellow}{fmt.fg.black}{tab.active_oldest_window.user_vars.cc_status_waiting}{fmt.bg.default}{fmt.fg.default}{fmt.bg.blue}{fmt.fg.white}{tab.active_oldest_window.user_vars.cc_status_done}{fmt.bg.default}{fmt.fg.default}{fmt.bg.cyan}{fmt.fg.black}{tab.active_oldest_window.user_vars.cc_status_ready}{fmt.bg.default}{fmt.fg.default}"

enable_keyboard_protocol no
env TERM=xterm-256color

# Required for nvim <-> kitty colorscheme sync (see lua/config/terminal-sync.lua)
allow_remote_control yes

background_opacity 1.0
dynamic_background_opacity yes

# BEGIN_KITTY_THEME
# Active palette: vague (driven by nvim's ColorScheme autocmd in
# lua/config/terminal-sync.lua, which copies ~/.config/kitty/vague.conf
# into current-theme.conf and overlays nvim's runtime highlights).
include current-theme.conf
# END_KITTY_THEME

# include ./gumshoe.conf
