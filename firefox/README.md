# mozilla inverted configs

Navigate to this folder:

	/home/arthur/snap/firefox/common/.mozilla/firefox/wgq2rp9r.default/chrome/

and, if it doesn't already exists, add the `userChrome.css` in this folder
